# 05. 배열 병합하기

## 1. 문제 정보

- [데일리알고 - 배열 병합하기](https://dailyalgo.kr/ko/problems/206)

## 2. 소요 시간

- 15분

## 3. 풀이 설명

### 💡 아이디어 도출 과정

- 주어진 두 배열 arr1, arr2는 이미 오름차순으로 정렬되어 있어 단순히 합친 뒤 Arrays.sort()를 호출하면 쉽게 풀 수 있지만, 이 경우 시간복잡도는 O((N+M) log(N+M)) 이 된다.
- 두 배열이 정렬되어 있으므로, 투 포인터를 이용해 한 번에 병합하고 더 효율적으로 풀 수 있다. 시간복잡도 O(N+M) 으로 더 효율적이다.

### ⚙️ 구현 로직의 동작 과정

1. 두 배열의 인덱스를 각각 가리키는 포인터 i, j를 선언하고 0으로 초기화한다.
    
    ```java
            int n = arr1.length, m = arr2.length;
            int i = 0, j = 0;
            List<Integer> merged = new ArrayList<>();
    ```
    
2. 두 배열의 현재 원소 중 작은 값을 결과 리스트에 추가한다. 작은 값을 선택한 쪽의 포인터를 1 증가시킨다.
    
    ```java
            while (i < n && j < m) {
                if (arr1[i] <= arr2[j]) {
                    merged.add(arr1[i]);
                    i++;
                } else {
                    merged.add(arr2[j]);
                    j++;
                }
            }
    ```
    
3. 어느 한쪽 배열이 끝나면, 남은 다른 배열의 원소를 전부 추가한다.
    
    ```java
            while (i < n) merged.add(arr1[i++]);
            while (j < m) merged.add(arr2[j++]);
    ```
    
4. 리스트를 배열로 변환하여 반환한다.
    
    ```java
    return merged.stream().mapToInt(Integer::intValue).toArray();
    ```
    

### ✅ 엣지 케이스에 대한 고려 사항

- 두 배열 중 하나가 빈 배열인 경우 → 그대로 다른 배열을 반환해야 함

## 4. 어려웠던 점 및 해결 과정

- 두 수를 비교하니깐 투 포인터를 활용하면 된다고 생각했다.

## 5. 궁금한 점

- Arrays.sort을 사용하는 것도 O(NlogN)이니 괜찮다고 생각하여 제출해봤습니다. 시간내에 풀이가 되었는데 데이터값이 어느정도로 많을때 시간초과가 발생할까요