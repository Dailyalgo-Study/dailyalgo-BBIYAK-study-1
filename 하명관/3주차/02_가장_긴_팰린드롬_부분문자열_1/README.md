# 02. 가장 긴 팰린드롬 부분문자열 1

## 1. 문제 정보

- [데일리알고 - 가장 긴 팰린드롬 부분문자열 1](https://dailyalgo.kr/ko/problems/200)

## 2. 소요 시간

- 20분

## 3. 풀이 설명

### 💡 아이디어 도출 과정

- 문자열중 한 부분을 중심으로 잡고 그 중심에서 양옆으로 확장하여 왼쪽 오른쪽의 값이 일치한지 찾으면 된다
- 이때 중심이 문자 사이인 경우는 짝수인 상황이고 중심 그자체인 경우는 홀수로 나온다.
- 짝수와 홀수인 두 경우를 검사하여 둘중 더 긴 길이를 갖고있는 경우를 찾으면 된다.

### ⚙️ 구현 로직의 동작 과정

- 문자열중 한 부분을 중심으로 잡고 그 중심에서 양옆으로 확장하여 왼쪽 오른쪽의 값이 일치한지 찾으면 된다
    
    ```java
        private int expand(String s, int left, int right) {
            while (left >= 0 && right < s.length() && s.charAt(left) == s.charAt(right)) {
                left--;
                right++;
            }
    
            return right - left - 1;
        }
    ```
    
- 이때 중심이 문자 사이인 경우는 짝수인 상황이고 중심 그자체인 경우는 홀수로 나온다.
- 짝수와 홀수인 두 경우를 검사하여 둘중 더 긴 길이를 갖고있는 경우를 찾으면 된다.
    
    ```java
            for (int center = 0; center < n; center++) {
                int len1 = expand(text, center, center);
    	            int len2 = expand(text, center, center + 1);
    
                maxLen = Math.max(maxLen, Math.max(len1, len2));
            }
    ```
    

### ✅ 엣지 케이스에 대한 고려 사항

- 만약 짝수인 경우는 현재 중심에서 + 1한 값을 right로 넣으면 된다.

## 4. 어려웠던 점 및 해결 과정

- text의 길이가 작아서 모든 요소를 탐색해도 되지만 효율적이지 않기에 다른 방법을 찾아봤습니다.

## 5. 궁금한 점

- 좋은 풀이 방법인지 궁금합니다.