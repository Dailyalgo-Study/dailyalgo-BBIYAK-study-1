# 05. 학점 분배

## 1. 문제 정보

- [데일리알고 - 학점 분배](https://dailyalgo.kr/ko/problems/179)

## 2. 소요 시간

- 15분

## 3. 풀이 설명

### 💡 아이디어 도출 과정

- 1 ~ n 까지의 번호가 부여된 n명의 학생들의 점수가 담긴 배열 scores가 주어진다.
- 상 중 하 등급을 점수가 높은 순으로 부여한다.
- 만약 점수가 같다면 학생 번호가 작은 순서대로 상위 등급 부여

### ⚙️ 구현 로직의 동작 과정

- (번호, 점수) 쌍을 저장하기 위한 2차원 배열 생성
    
    ```java
    int n = scores.length;
    int[][] students = new int[n][2];
    for (int i = 0; i < n; i++) {
        students[i][0] = i;         // 학생 번호
        students[i][1] = scores[i]; // 점수
    }
    ```
    
- 점수는 내리참순으로 정렬 만약 점수가 같다면 오름차순 정렬
    
    ```java
    Arrays.sort(students, (a, b) -> {
        if (a[1] == b[1]) return Integer.compare(a[0], b[0]);
        return Integer.compare(b[1], a[1]);
    });
    ```
    
- 등급 분배
    
    ```java
    int k = n / 3;
    String[] grades = new String[n];
    for (int i = 0; i < n; i++) {
        if (i < k) grades[students[i][0]] = "A";
        else if (i < 2 * k) grades[students[i][0]] = "B";
        else grades[students[i][0]] = "C";
    }
    
    ```
    

### ✅ 엣지 케이스에 대한 고려 사항

- 점수가 동일한 경우 학생 번호로 비교

## 4. 어려웠던 점 및 해결 과정

- 없습니다.

## 5. 궁금한 점

- 없습니다.